---
import type { Locale } from '@/i18n/config'
import type { Messages } from '@/i18n/types'
import { localePath } from '@/lib/site'

interface Props {
  messages: Messages
  locale: Locale
}
const { messages, locale } = Astro.props
const f = messages.faq
---

<section class="section" id="faq">
  <div class="container">
    <div class="narrow">
      <div class="section-head">
        <p class="eyebrow">{f.eyebrow}</p>
        <h2 class="type-section">{f.title}</h2>
      </div>
      <div class="list">
        {
          f.items.map((item, i) => (
            <details open={i === 0}>
              <summary>{item.q}</summary>
              <p>
                {item.a}
                {item.href && item.linkLabel ? (
                  <>
                    {' '}
                    <a
                      class="link-inline"
                      href={localePath(locale, item.href)}
                      data-journey-link={item.href === '/contact' ? 'contact' : undefined}
                    >{item.linkLabel}</a>.
                  </>
                ) : null}
              </p>
            </details>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  /* Rules, not boxes — a stack of bordered cards competes with the answers. */
  .list {
    border-top: 1px solid var(--line);
  }
  details {
    border-bottom: 1px solid var(--line);
  }
  summary {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: start;
    gap: 1rem;
    cursor: pointer;
    list-style: none;
    padding: 0.95rem 0;
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 700;
    letter-spacing: var(--tracking-display);
    line-height: var(--lh-snug);
  }
  summary::-webkit-details-marker {
    display: none;
  }
  summary::after {
    content: '';
    flex-shrink: 0;
    width: 0.5rem;
    height: 0.5rem;
    margin-right: 0.25rem;
    border-right: 2px solid var(--accent);
    border-bottom: 2px solid var(--accent);
    transform: rotate(45deg) translate(-25%, -25%);
    transition: transform 0.2s var(--ease);
  }
  details[open] summary::after {
    transform: rotate(-135deg) translate(-25%, -25%);
  }
  summary:hover {
    color: var(--accent-deep);
  }
  p {
    margin: 0 0 1.1rem;
    max-width: 42rem;
    color: var(--ink-muted);
    font-size: var(--text-sm);
  }
</style>
