---
import type { Locale } from '@/i18n/config'
import type { Messages } from '@/i18n/types'
import { localePath, whatsappUrl } from '@/lib/site'

interface Props {
  locale: Locale
  messages: Messages
}
const { locale, messages } = Astro.props
const c = messages.cta
const waHref = whatsappUrl(messages.contact.whatsappPrefill)
---

<section class="section">
  <div class="container">
    <div class="banner">
      <div>
        <h2 class="type-section">{c.title}</h2>
        <p class="type-lead">{c.subtitle}</p>
      </div>
      <div class="actions">
        <a class="btn btn-primary" href={waHref} target="_blank" rel="noopener noreferrer">
          {c.whatsapp}
        </a>
        <a class="btn btn-ghost" href={localePath(locale, '/contact')}>{c.button}</a>
      </div>
    </div>
  </div>
</section>

<style>
  .banner {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: flex-start;
    justify-content: space-between;
    padding: clamp(1.6rem, 4vw, 2.4rem);
    border-radius: calc(var(--radius-lg) + 4px);
    border: 1px solid rgba(15, 118, 110, 0.22);
    background:
      radial-gradient(600px 200px at 10% 0%, rgba(20, 184, 166, 0.18), transparent 55%),
      linear-gradient(135deg, #ffffff, #e8f7f4);
    box-shadow: var(--shadow);
  }
  h2 {
    max-width: 18ch;
  }
  p {
    margin: 0.65rem 0 0;
    color: var(--ink-muted);
    max-width: 36rem;
  }
  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    flex-shrink: 0;
  }
  @media (min-width: 800px) {
    .banner {
      flex-direction: row;
      align-items: center;
    }
  }
</style>
