---
import type { Locale } from '@/i18n/config'
import type { Messages } from '@/i18n/types'
import LinkCard from '@/components/LinkCard.astro'
import { localePath } from '@/lib/site'

interface Props {
  locale: Locale
  messages: Messages
}
const { locale, messages } = Astro.props
const e = messages.eeat
---

<section class="section" id="trust" aria-labelledby="trust-title">
  <div class="container">
    <div class="section-head">
      <p class="eyebrow">{e.eyebrow}</p>
      <h2 id="trust-title" class="type-section">
        {e.title} <span class="accent">{e.titleAccent}</span>
      </h2>
      <p class="lead">{e.intro}</p>
    </div>
    <ul class="card-grid">
      {
        e.items.map((item) => (
          <li>
            <LinkCard href={localePath(locale, item.href)} title={item.title} desc={item.desc} />
          </li>
        ))
      }
    </ul>
  </div>
</section>
