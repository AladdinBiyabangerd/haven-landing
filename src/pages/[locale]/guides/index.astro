---
import BaseLayout from '@/layouts/BaseLayout.astro'
import Cta from '@/components/Cta.astro'
import { resolveLocale } from '@/i18n/config'
import { getMessages } from '@/i18n'
import { getGuides, getGuidesHub } from '@/data/guides'
import { localePath } from '@/lib/site'
import { truncateText } from '@/lib/format'

export function getStaticPaths() {
  return [{ params: { locale: 'az' } }, { params: { locale: 'en' } }, { params: { locale: 'ru' } }]
}

const locale = resolveLocale(Astro.params.locale)
const messages = getMessages(locale)
const hub = getGuidesHub(locale)
const items = getGuides(locale)
---

<BaseLayout
  locale={locale}
  messages={messages}
  page="guides"
  seo={{
    title: hub.seoTitle,
    description: hub.seoDescription,
    keywords: hub.keywords,
    slug: '/guides',
    breadcrumbName: messages.nav.guides,
  }}
>
  <section class="section">
    <div class="container">
      <header class="section-head">
        <p class="eyebrow">{hub.eyebrow}</p>
        <h1 class="type-page-title">{hub.h1}</h1>
        <p class="lead">{hub.intro}</p>
      </header>
      <ul class="cards">
        {
          items.map((item) => (
            <li>
              <article>
                <h2 class="type-card-title">{item.shortTitle}</h2>
                <p>{truncateText(item.intro, 180, messages.common.truncatedEllipsis)}</p>
                <a href={localePath(locale, `/guides/${item.slug}`)}>
                  {item.h1}
                </a>
              </article>
            </li>
          ))
        }
      </ul>
      <p class="solutions-link">
        <a href={localePath(locale, '/solutions')}>{messages.nav.solutions}{messages.common.linkArrow}</a>
      </p>
    </div>
  </section>
  <Cta locale={locale} messages={messages} intent="next" />
</BaseLayout>

<style>
  .cards {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }
  article {
    padding: 1.35rem 1.25rem;
    border-radius: var(--radius-lg);
    border: 1px solid var(--line);
    background: var(--surface);
    display: grid;
    gap: 0.65rem;
  }
  p {
    margin: 0;
    color: var(--ink-muted);
  }
  a {
    font-weight: 600;
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 0.2em;
  }
  .solutions-link {
    margin: 1.5rem 0 0;
  }
  @media (min-width: 800px) {
    .cards {
      grid-template-columns: 1fr;
      max-width: 760px;
    }
  }
</style>
